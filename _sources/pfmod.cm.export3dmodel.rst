Export 3D Model
---------------
This provides a means to export the model in one of the following formats:

* **NPZ** - This is the format used by PyGMI, so the model must be saved in this format if you want to use it in PyGMI.
* **SHP** - This exports each lithology to a separate shapefile.
* **KMZ** - For use in Google Earth. All the datasets and the model are included in the exported file.
* **CSV** - Exports the model to a **CSV** file.

.. figure:: _images/pfmodgearth.png

   A 3D model created in PyGMI shown in Google Earth. The magnetic data is shown with transparency added.
